<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thanh toán qua QR</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">
    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
        <h1 class="text-2xl font-bold mb-6 text-center">Thanh toán qua QR</h1>

        <div class="mb-6">
            <p><strong>Mã đơn hàng:</strong> <%= orderId %></p>
            <p><strong>Tổng tiền:</strong> <%= totalAmount.toLocaleString('vi-VN') %> VNĐ</p>
        </div>

        <div class="mb-6 text-center">
            <p class="mb-2">Quét mã QR để thanh toán:</p>
            <!-- Sử dụng ảnh tĩnh cho QR Code -->
            <img src="/images/momoQR.png" alt="QR Code" class="mx-auto">
        </div>

        <div class="mb-6">
            <p class="text-center text-gray-500">Sau khi thanh toán, nhập mã giao dịch từ ứng dụng thanh toán của bạn:</p>
        </div>

        <form action="/customer/confirm-payment/<%= orderId %>" method="POST" class="space-y-4">
            <div>
                <label for="transactionCode" class="block text-sm font-medium text-gray-700">Mã giao dịch:</label>
                <input type="text" id="transactionCode" name="transactionCode" placeholder="Nhập mã giao dịch" class="mt-1 block w-full p-2 border border-gray-300 rounded-md" required>
            </div>
            <button type="submit" class="w-full bg-green-500 text-white px-4 py-2 rounded hover:bg-green-600">Tôi đã hoàn tất thanh toán trên App</button>
        </form>

        <div class="mt-6 text-center">
            <a href="/customer/don-hang" class="text-blue-500 hover:underline">Quay lại đơn hàng</a>
        </div>
    </div>
</body>
</html>